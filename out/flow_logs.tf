# Generated by Transcribe - SOC2 Compliant Architecture

# ── VPC Flow Logs (SOC2 CC6.6 / CC7.2) ──────────────────────────────────────

resource "aws_iam_role" "myapp_flow_logs" {
  name = "${local.env}-myapp-vpc-flow-logs-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Service = "vpc-flow-logs.amazonaws.com" }
      Action    = "sts:AssumeRole"
    }]
  })

  tags = {
    Name = "${local.env}-myapp-vpc-flow-logs-role"
  }
}

resource "aws_iam_role_policy" "myapp_flow_logs" {
  name = "${local.env}-myapp-vpc-flow-logs-policy"
  role = aws_iam_role.myapp_flow_logs.id

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect = "Allow"
      Action = [
        "logs:CreateLogGroup",
        "logs:CreateLogStream",
        "logs:PutLogEvents",
        "logs:DescribeLogGroups",
        "logs:DescribeLogStreams",
      ]
      Resource = "*"
    }]
  })
}

resource "aws_flow_log" "myapp" {
  vpc_id          = aws_vpc.myapp.id
  traffic_type    = "ALL"
  iam_role_arn    = aws_iam_role.myapp_flow_logs.arn
  log_destination = aws_cloudwatch_log_group.myapp_flow_logs.arn

  tags = {
    Name = "myapp-vpc-flow-logs"
  }
}
